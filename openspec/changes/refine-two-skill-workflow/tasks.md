## 1. Contract and baseline

- [x] 1.1 Record the user's implementation/publication approval and Gate 0 classification.
- [x] 1.2 Create and verify structured backups of both source and runtime skill trees.
- [x] 1.3 Run read-only routing, integration, and closure baseline scenarios.
- [x] 1.4 Validate this OpenSpec change with strict mode.

## 2. Tests and implementation

- [x] 2.1 Add failing routing and lifecycle regression tests.
- [x] 2.2 Refine `openspec-superpower-change` routing, Superpowers boundary, review loop, and final completion rules.
- [x] 2.3 Refine `codex-brief-antigravity-review` standalone/handed-off modes, attempt loop, templates, and metadata.
- [x] 2.4 Upgrade the canonical Handoff Contract and both validators.
- [x] 2.5 Fix YAML fallback parsing and validator target-path handling.
- [x] 2.6 Update README, AGENTS, examples, and design documentation without unrelated formatting.

## 3. Synchronization and verification

- [x] 3.1 Run both repository validators and standard-library test suites.
- [x] 3.2 Synchronize both installed runtime skill copies and verify source/runtime parity.
- [x] 3.3 Run `quick_validate.py` and project validators on all source/runtime copies.
- [x] 3.4 Run GREEN forward-tests for routing and closure scenarios.
- [x] 3.5 Review final diffs, scope, sensitive data, temporary files, and rollback readiness.

## 4. Publication

- [ ] 4.1 Commit each repository separately with scoped Chinese Conventional Commit messages.
- [ ] 4.2 Push both `main` branches to `origin` and verify tracking status.
- [ ] 4.3 Remove temporary backups only after validation and push succeed.
