# Tasks: add-project-learning-gate

## 1. Proposal and approval

- [x] 1.1 Audit current Discovery First, context checkpoint, learning-candidate,
  completion, and cross-CLI rules plus the qagent ignored-context evidence.
- [x] 1.2 Create the Major Self-Evolution review draft and structured backup.
- [x] 1.3 Create proposal, design, tasks, and governance spec delta artifacts.
- [x] 1.4 Run strict OpenSpec validation and fix every finding.
- [x] 1.5 Present the exact change-id and record explicit approval of this scoped
  contract before implementation.

## 2. RED contract and forward tests

- [x] 2.1 Add router tests that require the Domain Context Check in the entry
  matrix, conditional `grill-with-docs`, and a complete cross-CLI fallback.
- [x] 2.2 Add tests for automatic project-local thresholds, explicit
  archive/distill closeout, Candidate Card provenance, and completion blocking.
- [x] 2.3 Add tests that keep implementation detail out of `CONTEXT.md`, route it
  to engineering-invariant guidance, and require deterministic enforcement when
  mechanically possible.
- [x] 2.4 Add a sanitized qagent-shaped fixture and preserve focused RED output
  before portable implementation files change.
- [x] 2.5 Add isolated forward scenarios for a clear localized task, ambiguous
  domain language, repeated correction plus independent Review, single low-risk
  correction, and explicit session archive/distillation.

## 3. GREEN source implementation

- [x] 3.1 Update router entry, phase-aware routing, completion, Non-negotiables,
  and reference matrix without weakening existing approval or evidence gates.
- [x] 3.2 Update request modes, local-instruction checkpoint, learning-candidate
  pipeline, and approved closeout ordering; add project-promotion guidance and a
  Candidate Card template.
- [x] 3.3 Update the managed governance rule, portable manifest, core validator,
  and sync validator with stable cross-CLI semantics.
- [x] 3.4 Update examples, changelog, and English/Chinese README workflow details
  including the explicit archive/distill trigger and artifact boundaries.
- [x] 3.5 Run focused tests to establish GREEN and preserve the RED/GREEN report.

## 4. Source validation and Review

- [x] 4.1 Run quick validation, dependency-free core validation, full unit tests,
  strict OpenSpec validation, and diff checks.
- [x] 4.2 Run fresh isolated forward-tests; do not reuse the earlier adaptive-
  routing scenarios as proof for the learning gate.
- [x] 4.3 Run distinct High Review over routing order, completion blocking,
  knowledge classification, sensitive-data safety, Git authority, and full diff.
- [x] 4.4 Fix every finding and repeat verification/Review until PASS.

## 5. Runtime synchronization

- [x] 5.1 Generate and Review a path/hash-only sync plan from the new validated
  canonical source.
- [x] 5.2 Apply and verify Codex, Antigravity CLI, and Grok CLI one target at a
  time with rollback on failure.
- [x] 5.3 Run verify-all, discovery checks, and final cross-target Review.

## 6. Project Learning Closeout and publication

- [x] 6.1 Audit this change's own corrections and Review findings; promote any
  confirmed project-local lesson before final verification.
- [x] 6.2 Run fresh final critical validation and final diff/scope/sensitive-data
  Review after learning promotion.
- [x] 6.3 Reconcile tasks, archive the change, and validate strictly after archive.
- [x] 6.4 Complete the already authorized commit/cherry-pick/push workflow only
  after both changes pass their final gates.
- [ ] 6.5 Remove temporary backups and extra feature worktrees only after remote
  verification; then provide the distilled session archive and final summary.
